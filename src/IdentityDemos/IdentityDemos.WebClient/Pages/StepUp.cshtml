@page
@model IdentityDemos.WebClient.Pages.StepUpModel
@{
}

<h1>@ViewData["Title"]</h1>

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>
